<div class="bg-[#1e1e1e] text-white rounded-2xl shadow-2xl w-full max-w-lg p-8 relative border border-[#2c2c2c] overflow-hidden">


    <!-- Close Button -->
    <button class="absolute top-4 right-4 text-gray-400 hover:text-[#db352b] transition-all duration-200 close-modal">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
            stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
    </button>

    <!-- Header -->
    <div class="text-center mb-8 border-b border-[#2c2c2c] pb-5">
        <h1 class="text-3xl font-oswald font-bold text-[#db352b] tracking-wide uppercase drop-shadow-md">
            Edit Customer
        </h1>
        <p class="text-sm text-gray-400 mt-2">Update customer details and save changes</p>
    </div>

    <!-- Edit Form -->
    <div class="space-y-6 relative z-10">
        {{ include('customer/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <!-- Bottom Accent Bar -->
    <div class="absolute bottom-0 left-0 w-full h-[3px] bg-gradient-to-r from-[#db352b] via-[#ff5b4a] to-[#db352b]"></div>

</div>
